
<%= simple_form_for(@controller_action) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :action %>
    <%= f.input :controller %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
